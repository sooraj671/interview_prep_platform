[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main_orm.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8000"
APP_ENV = "production"
